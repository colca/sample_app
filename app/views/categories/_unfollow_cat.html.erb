<%= form_for(current_user.user_cat_relationships.find_by_category_id(@category), 
             html: { method: :delete }, 
             remote: true) do |f| %>
<%= f.submit "Unfollow #{@category.cName} Genre", class: "btn btn-large" %>
<% end %>
