<%= form_for(current_user.user_dancer_relationships.find_by_dancer_id(@dancer), 
             html: { method: :delete }, 
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-small" %>
<% end %>
