<%= form_for(current_user.user_dancer_relationships.build(dancer_id: @dancer.id), 
             remote: true) do |f| %>
  <div><%= f.hidden_field :dancer_id %></div>
  <%= f.submit "Follow", class: "btn btn-small btn-primary" %>
<% end %>
